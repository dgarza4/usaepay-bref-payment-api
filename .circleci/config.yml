version: 2
jobs:
    build:
      working_directory: ~/project
      docker:
        - image: circleci/node:10
      steps:
        - checkout
        - run:
            name: "Build Api on lambda"
            command: |
             cd ~/project
             npm install -g serverless
             serveless deploy